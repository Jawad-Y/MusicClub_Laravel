<?php

namespace Tests\Feature\Auth;

use Tests\TestCase;

/**
 * Email verification tests are skipped for API-only applications.
 * Email verification in API context is handled differently.
 */
class EmailVerificationTest extends TestCase
{
    // All tests skipped - not applicable for API-only application
}
